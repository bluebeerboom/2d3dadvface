# 2026-06-26

## 修复 ui_sign_v2.py 无法启动
- **原因**: `src/__init__.py` 在包初始化时强制 `from .models import ArcFaceResNet50`，但 `src/models/` 目录缺少 `__init__.py`（模型代码在嵌套 `src/models/models/` 下），导致整个 `src` 包 ImportError
- **修复**: 移除 `src/__init__.py` 中的强制导入，改为注释说明需要时直接导入
- 程序修复后正常启动（PyQt5 GUI窗口正常弹出）
